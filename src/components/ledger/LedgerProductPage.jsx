import React from 'react';
import LedgerDashboardPage from '../../pages/ledger/LedgerDashboardPage';

/**
 * KEPWE LEDGER is the authenticated live financial management platform.
 * Backed by real database transactions, accounts, invoices, bills, and reports.
 */
const LedgerProductPage = () => <LedgerDashboardPage />;

export default LedgerProductPage;